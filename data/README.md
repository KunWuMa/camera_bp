# Data directory

This directory contains documentation only. Real datasets must remain outside
version control even when they are copied here locally for execution.

- `private/README.md`: governed hospital and laboratory cohorts.
- `public/rppg_bp_ukl/README.md`: original public download source and expected path.

The root `.gitignore` excludes every file below `data/` except README files.
